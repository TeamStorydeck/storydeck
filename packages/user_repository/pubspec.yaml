name: user_repository
description: A starting point for Dart libraries or applications.
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^1.1.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
